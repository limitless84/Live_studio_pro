import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Palette (Confirmed)
  static const Color navyBlue = Color(0xFF0A3442); // آبی نفتی
  static const Color cloudWhite = Color(0xFFF4F6F9); // سفید ابری
  static const Color cloudGray = Color(0xFFCED7DE); // خاکستری ابری

  // Semantic
  static const Color primary = navyBlue;
  static const Color background = cloudWhite;
  static const Color surface = cloudWhite;
  static const Color border = cloudGray;

  static const Color textPrimary = Color(0xFF0D1B22);
  static const Color textSecondary = Color(0xFF4A5B65);

  // Accent (خیلی ملایم برای استفاده محدود)
  static const Color accent = Color(0xFF1C5566);
}

